<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AliasDisplay" parent="@android:style/TextAppearance.Small">
        <item name="fontPath">Effra-Medium.ttf</item>
        <item name="android:layout_width">@dimen/chat_bubble_min_height</item>
        <item name="android:layout_height">@dimen/chat_bubble_min_height</item>
        <item name="android:layout_alignBottom">@+id/body</item>
        <item name="android:background">@drawable/chat_bubble_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@android:color/primary_text_dark</item>
        <item name="android:textSize">@dimen/chat_bubble_author_text_size</item>
    </style>

</resources>
